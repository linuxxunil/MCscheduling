<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.mcscheduling"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- user permission -->
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
	
    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="16" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="edu.mcscheduling.controller.HomeActivity"
            android:label="@string/app_name" >
        </activity>
  
        <activity
            android:name="edu.mcscheduling.controller.EnrollActivity"
            android:label="@string/app_name" />

        <activity
            android:name="edu.mcscheduling.controller.LoginActivity"
            android:label="@string/app_name" />      
           
		<activity
            android:name="edu.mcscheduling.controller.MenuActivity"
            android:label="@string/app_name" />  
			
        <activity
            android:name="edu.mcscheduling.controller.RevisePasswordActivity"
            android:label="@string/app_name" />       

        <activity
            android:name="edu.mcscheduling.controller.TestActivity"
            android:label="@string/app_name">  
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
         </activity>
    </application>

</manifest>